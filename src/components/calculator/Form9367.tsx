import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { CalculatorSectionProps } from './types';

const Form9367 = ({ calculator }: CalculatorSectionProps) => {
  return (
    <Card className="bg-white/10 backdrop-blur-sm border-white/20 text-white">
      <CardHeader>
        <CardTitle>Tiered Performance Path</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Tiered Performance path form is under construction.</p>
      </CardContent>
    </Card>
  );
};

export default Form9367;